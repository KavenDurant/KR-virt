{
  "name": "kr-virt-ssh-proxy",
  "version": "1.0.0",
  "description": "SSH代理服务器，为KR-virt提供SSH终端功能",
  "main": "ssh-proxy-server.js",
  "scripts": {
    "start": "node ssh-proxy-server.js",
    "dev": "nodemon ssh-proxy-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ssh",
    "websocket",
    "terminal",
    "proxy"
  ],
  "author": "KavenDurant",
  "license": "MIT",
  "dependencies": {
    "ssh2": "^1.15.0",
    "ws": "^8.16.0",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
